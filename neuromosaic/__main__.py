"""
Main entry point for the neuromosaic package when run as a module.

This allows running the package with `python -m neuromosaic`.
"""

from neuromosaic.cli import main

if __name__ == "__main__":
    main()
